import { Box, Typography } from "@mui/material";

export default function Header() {
  return (
    <Box sx={{ height: 60, width: "300px", marginTop: "20px" }}>
      <Typography variant="h4">Desire Music Club</Typography>
    </Box>
  );
}
