import { Box, Typography } from "@mui/material";

export default function Header() {
  return (
    <Box sx={{ height: 60 }}>
      <Typography variant="h4">Desire Music Club</Typography>
    </Box>
  );
}
